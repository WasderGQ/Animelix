using UnityEngine;

namespace ApplicationPanels._01_VideoPanel._11_EpisodesList
{
    public class EpisodeListController : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
